using UnityEngine;

public class PixelsShelf : MonoBehaviour
{
    private void Awake()
    {
        //transform.position = new Vector3(transform.position.x, -(CameraFit.SCREEN_WIDTH / 2f), transform.position.z);
    }
}
